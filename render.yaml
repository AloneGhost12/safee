services:
  # Main API Service
  - type: web
    name: vault-api
    runtime: node
    plan: starter
    buildCommand: cd server && npm run build:render
    startCommand: cd server && npm run start:production
    rootDir: .
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 4000
      - key: HOST
        value: 0.0.0.0
      - key: MONGO_URI
        sync: false
      # Note: Set MONGO_URI manually in Render dashboard with value:
      # mongodb+srv://adharshnp92:OjMNYzmnZnR4sVnl@cluster0.vrx397v.mongodb.net/?retryWrites=true&w=majority&appName=Cluster0
      - key: JWT_ACCESS_SECRET
        generateValue: true
      - key: JWT_REFRESH_SECRET
        generateValue: true
      - key: SESSION_COOKIE_NAME
        value: pv_sess
      - key: ALLOWED_ORIGINS
        value: https://aloneghost12.github.io,https://aloneghost12.netlify.app
      - key: TRUST_PROXY
        value: true
      - key: TRUSTED_PROXIES
        value: 127.0.0.1,::1,172.68.0.0/12,172.69.0.0/12,104.16.0.0/12
      # Cloudinary configuration
      - key: CLOUDINARY_CLOUD_NAME
        value: dtzhskby3
      - key: CLOUDINARY_API_KEY
        value: 279872768346294
      - key: CLOUDINARY_API_SECRET
        value: R2ioEHtBBs-6Kwmj-pbCL5Er3CQ
      # S3/R2 (optional)
      - key: S3_ENDPOINT
        sync: false
      - key: S3_BUCKET
        sync: false
      - key: S3_REGION
        sync: false
      - key: S3_ACCESS_KEY_ID
        sync: false
      - key: S3_SECRET_ACCESS_KEY
        sync: false
    healthCheckPath: /api/health

# Note: Render doesn't support MongoDB as a managed database
# Use MongoDB Atlas for the database service
# databases:
#   - name: vault-mongodb
#     databaseName: vault
#     user: vault_user
#     plan: starter
